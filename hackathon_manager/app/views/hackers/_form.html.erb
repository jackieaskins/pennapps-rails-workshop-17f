<%= form_with(model: hacker, local: true) do |form| %>
  <% if hacker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hacker.errors.count, "error") %> prohibited this hacker from being saved:</h2>

      <ul>
      <% hacker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name, id: :hacker_first_name %>
  </div>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name, id: :hacker_last_name %>
  </div>

  <div class="field">
    <%= form.label :university %>
    <%= form.text_field :university, id: :hacker_university %>
  </div>

  <div class="field">
    <%= form.label :hours_of_sleep %>
    <%= form.number_field :hours_of_sleep, id: :hacker_hours_of_sleep %>
  </div>

  <div class="field">
    <%= form.label :project %>
    <%= form.number_field :project_id, id: :hacker_project %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
